{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint",
    "test": "node -e \"const fs=require('fs');const c=fs.readFileSync('src/App.jsx','utf8');const s1=(c.match(/<select/g)||[]).length;const s2=(c.match(/<select className=\\\"text-input\\\"/g)||[]).length;if(s1!==s2){console.error('REGRESSION: Found ' + s1 + ' <select> tags but only ' + s2 + ' have className=\\\"text-input\\\"');process.exit(1)}console.log('All select tags have text-input');process.exit(0)\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "lucide-react": "^1.23.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "recharts": "^3.9.2"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "eslint": "^10.7.0",
    "oxlint": "^1.71.0",
    "vite": "^8.1.1"
  }
}
